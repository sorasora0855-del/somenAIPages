const API_BASE=location.hostname.endsWith('.github.io')?'https://123456654329.asro.jp':'';const $=id=>document.getElementById(id);const sidebar=$('sidebar'),chatList=$('chatList'),chat=$('chat'),composer=$('composer'),input=$('input'),send=$('send'),attachButton=$('attach'),imageInput=$('imageInput'),cameraInput=$('cameraInput'),attachmentPreview=$('attachmentPreview'),status=$('status');let currentChatId=null,pendingImages=[],requestController=null,imageMode=false;
async function api(path,options={}){const res=await fetch(API_BASE+path,{credentials:'include',...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});const data=await res.json().catch(()=>({}));if(!res.ok)throw Error(data.error||`HTTP ${res.status}`);return data}
function toast(msg,bad=false){status.textContent=msg;status.style.color=bad?'#b4232f':'#777';setTimeout(()=>{if(status.textContent===msg)status.textContent=''},2600)}
function emptyChat(){chat.innerHTML='<div class="welcome"><h1>こんにちは！</h1><p>somenAIに何でも聞いてみてね。</p></div>'}
function renderChatList(chats){chatList.innerHTML='';for(const c of chats){const row=document.createElement('div');row.className='chat-row'+(c.id===currentChatId?' active':'');const title=document.createElement('div');title.className='chat-title';title.textContent=c.title||'新しいチャット';const more=document.createElement('button');more.className='chat-more';more.textContent='•••';more.onclick=e=>{e.stopPropagation();chatMenu(c)};row.append(title,more);row.onclick=()=>openChat(c.id);chatList.appendChild(row)}}
async function refreshChats(){const d=await api('/api/chats');renderChatList(d.chats);return d.chats}
async function createChat(){const d=await api('/api/chats',{method:'POST',body:'{}'});currentChatId=d.chat.id;emptyChat();await refreshChats();closeMobile();input.focus()}
async function openChat(id){currentChatId=id;const d=await api(`/api/chats/${id}`);chat.innerHTML='';if(!d.messages?.length)emptyChat();else d.messages.forEach(m=>{if(m.role==='user'&&m.image)addUserImageMessage(m.content||'',m.image);else addTextMessage(m.role,m.content||'')});await refreshChats();closeMobile();input.focus()}
function addTextMessage(role,text){document.querySelector('.welcome')?.remove();const el=document.createElement('article');el.className='message '+role;el.dataset.raw=text;el.textContent=text;chat.appendChild(el);if(role==='assistant'){const a=document.createElement('div');a.className='message-actions';const b=document.createElement('button');b.className='message-action';b.textContent='コピー';b.onclick=()=>navigator.clipboard?.writeText(el.dataset.raw||'').then(()=>{b.textContent='コピー済み';setTimeout(()=>b.textContent='コピー',1000)});a.appendChild(b);el.appendChild(a)}chat.scrollTop=chat.scrollHeight;return el}
function imageSrc(x){if(typeof x==='string')return x;return x?.url||x?.data||x?.base64||''}
function addUserImageMessage(text,images){document.querySelector('.welcome')?.remove();const el=document.createElement('article');el.className='message user user-image-message';if(text){const p=document.createElement('p');p.textContent=text;el.appendChild(p)}const grid=document.createElement('div');grid.className='user-image-grid';const arr=Array.isArray(images)?images:[images];for(const x of arr){const src=imageSrc(x);if(!src)continue;const img=document.createElement('img');img.src=src;img.alt='送信画像';img.loading='lazy';grid.appendChild(img)}el.appendChild(grid);chat.appendChild(el);chat.scrollTop=chat.scrollHeight;return el}
function renderAttachments(){attachmentPreview.innerHTML='';if(!pendingImages.length){attachmentPreview.classList.add('hidden');return}attachmentPreview.classList.remove('hidden');const head=document.createElement('div');head.className='attachment-head';head.innerHTML='<span>画像</span><span>'+pendingImages.length+'/2</span>';attachmentPreview.appendChild(head);const grid=document.createElement('div');grid.className='attachment-grid';pendingImages.forEach((f,i)=>{const item=document.createElement('div');item.className='attachment-item';const img=document.createElement('img');img.src=f.base64;const n=document.createElement('span');n.className='attachment-number';n.textContent=i+1;const x=document.createElement('button');x.className='attachment-remove';x.textContent='×';x.onclick=()=>{pendingImages.splice(i,1);renderAttachments()};item.append(img,n,x);grid.appendChild(item)});attachmentPreview.appendChild(grid)}
function addFiles(list){const incoming=[...list].filter(f=>f.type.startsWith('image/'));const room=2-pendingImages.length;pendingImages.push(...incoming.slice(0,room));renderAttachments();if(incoming.length>room)toast('画像は2枚までだよ',true)}
attachButton.onclick=()=>openPicker();function openPicker(){const back=document.createElement('div');back.className='modal-backdrop';const box=document.createElement('section');box.className='modal';const close=document.createElement('button');close.className='modal-close';close.textContent='×';const h=document.createElement('h2');h.textContent='画像を追加';const p=document.createElement('p');p.className='modal-subtitle';p.textContent='最大2枚まで添付できます';const files=document.createElement('button');files.className='primary';files.style.cssText='width:100%';files.textContent='🖼️ 写真・画像を選ぶ';const camera=document.createElement('button');camera.style.cssText='width:100%;margin-top:8px;padding:11px;border:1px solid #ddd;border-radius:9px;background:#fff';camera.textContent='📷 カメラで撮る';box.append(close,h,p,files,camera);back.appendChild(box);document.body.appendChild(back);const done=()=>back.remove();close.onclick=done;back.onclick=e=>{if(e.target===back)done()};files.onclick=()=>{done();imageInput.click()};camera.onclick=()=>{done();cameraInput.click()}}
imageInput.onchange=e=>{addFiles(e.target.files);e.target.value=''};cameraInput.onchange=e=>{addFiles(e.target.files);e.target.value=''};
function closeMobile(){sidebar?.classList.remove('open');$('mobileBackdrop')?.classList.remove('open')}
$('mobileMenu')?.addEventListener('click',()=>{sidebar.classList.add('open');$('mobileBackdrop')?.classList.add('open')});$('mobileBackdrop')?.addEventListener('click',closeMobile);$('newChat').onclick=()=>createChat().catch(showError);$('newChatMobile')?.addEventListener('click',()=>createChat().catch(showError));
function settingsModal(){const back=document.createElement('div');back.className='modal-backdrop';const box=document.createElement('section');box.className='modal';const close=document.createElement('button');close.className='modal-close';close.textContent='×';const h=document.createElement('h2');h.textContent='設定';const p=document.createElement('p');p.className='modal-subtitle';p.textContent='SomenAIの動作を設定';const modelLabel=document.createElement('label');modelLabel.textContent='AIプロバイダー';const select=document.createElement('select');['auto','gemini','groq'].forEach(v=>{const o=document.createElement('option');o.value=v;o.textContent=v==='auto'?'自動（おすすめ）':v==='gemini'?'Gemini':'Groq / Qwen';select.appendChild(o)});const webLabel=document.createElement('label');webLabel.className='check';webLabel.innerHTML='<input type="checkbox"> Web検索を使う';const ragLabel=document.createElement('label');ragLabel.className='check';ragLabel.innerHTML='<input type="checkbox"> 参考資料を使う';const actions=document.createElement('div');actions.className='modal-actions';const cancel=document.createElement('button');cancel.textContent='閉じる';actions.appendChild(cancel);box.append(close,h,p,modelLabel,select,webLabel,ragLabel,actions);back.appendChild(box);document.body.appendChild(back);close.onclick=cancel.onclick=()=>back.remove();back.onclick=e=>{if(e.target===back)back.remove()};api('/api/auth/me').then(d=>{select.value=d.user?.provider||'auto'}).catch(()=>{});select.onchange=()=>api('/api/settings/ai',{method:'PATCH',body:JSON.stringify({provider:select.value})}).catch(showError);}
$('settings')?.addEventListener('click',settingsModal);$('mobileSettings')?.addEventListener('click',settingsModal);
function chatMenu(c){const back=document.createElement('div');back.className='modal-backdrop';const box=document.createElement('section');box.className='modal';const h=document.createElement('h2');h.textContent='チャット';const rename=document.createElement('button');rename.className='primary';rename.style.cssText='width:100%;margin-top:14px';rename.textContent='✏️ 名前を変更';const del=document.createElement('button');del.style.cssText='width:100%;margin-top:8px;padding:11px;border:1px solid #ddd;border-radius:9px;background:#fff;color:#b4232f';del.textContent='🗑️ チャットを削除';box.append(h,rename,del);back.appendChild(box);document.body.appendChild(back);back.onclick=e=>{if(e.target===back)back.remove()};rename.onclick=async()=>{const title=prompt('新しいチャット名',c.title);if(title?.trim()){await api(`/api/chats/${c.id}`,{method:'PATCH',body:JSON.stringify({title:title.trim()})});await refreshChats()}back.remove()};del.onclick=async()=>{if(confirm('このチャットを削除しますか？')){await api(`/api/chats/${c.id}`,{method:'DELETE'});if(currentChatId===c.id)currentChatId=null;await refreshChats();const cs=await api('/api/chats');if(cs.chats.length)await openChat(cs.chats[0].id);else await createChat();back.remove()}}}
function showError(e){toast('エラー: '+e.message,true)}
composer.onsubmit=async e=>{e.preventDefault();const text=input.value.trim();if(imageMode){if(!text)return toast('画像の説明を入力してね',true);input.value='';try{await generateImage(text)}catch(err){showError(err)}return}if(!text&&!pendingImages.length)return;if(!currentChatId)return showError(Error('チャットを準備中です'));const files=pendingImages.slice();pendingImages=[];renderAttachments();input.value='';input.style.height='auto';if(files.length)addUserImageMessage(text,files.map(f=>f.base64));else addTextMessage('user',text);send.disabled=true;input.disabled=true;attachButton.disabled=true;requestController=new AbortController();status.textContent='生成中…';try{const images=files.map(f=>({mimeType:f.mimeType,base64:f.base64}));const res=await fetch(API_BASE+`/api/chats/${currentChatId}/messages/stream`,{method:'POST',credentials:'include',headers:{'Content-Type':'application/json','Accept':'text/event-stream'},body:JSON.stringify({message:text,image:images.length===1?images[0]:images.length?{mimeType:'image/jpeg',base64:await mergeImages(images)}:undefined,webSearch:false,useReferences:false}),signal:requestController.signal});if(!res.ok)throw Error(`HTTP ${res.status}`);const reader=res.body.getReader(),decoder=new TextDecoder();let buf='',ai=null;while(true){const {value,done}=await reader.read();if(done)break;buf+=decoder.decode(value,{stream:true});const parts=buf.split('\n\n');buf=parts.pop()||'';for(const part of parts){const line=part.split('\n').find(x=>x.startsWith('data: '));if(!line)continue;const d=JSON.parse(line.slice(6));if(d.notice)status.textContent=d.notice;if(d.text){if(!ai)ai=addTextMessage('assistant','');ai.dataset.raw+=d.text;const actions=ai.querySelector('.message-actions');ai.textContent=ai.dataset.raw;if(actions)ai.appendChild(actions)}if(d.done)status.textContent=''}}}catch(err){if(err.name!=='AbortError')showError(err)}finally{requestController=null;send.disabled=false;input.disabled=false;attachButton.disabled=false;input.focus();await refreshChats()}};
async function mergeImages(images){if(images.length===1)return images[0].base64;const bitmaps=await Promise.all(images.map(x=>createImageBitmap(dataURLToBlob(x.base64))));const width=Math.max(...bitmaps.map(b=>b.width));const height=bitmaps.reduce((s,b)=>s+b.height,0)+8*(bitmaps.length-1);const canvas=document.createElement('canvas');canvas.width=width;canvas.height=height;const ctx=canvas.getContext('2d');let y=0;for(const b of bitmaps){ctx.drawImage(b,0,y);y+=b.height+8}return canvas.toDataURL('image/jpeg',.88)}
function dataURLToBlob(data){const [meta,raw]=data.split(',');const bin=atob(raw);const arr=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)arr[i]=bin.charCodeAt(i);return new Blob([arr],{type:meta.match(/data:(.*?);/)?.[1]||'image/jpeg'})}
async function generateImage(prompt){send.disabled=true;status.textContent='画像を生成中…';try{const d=await api('/api/image/generate',{method:'POST',body:JSON.stringify({prompt,chatId:currentChatId})});const url=d.url||d.image?.url||d.image;if(!url)throw Error('画像生成結果がありません');addTextMessage('user','🎨 '+prompt);const el=document.createElement('article');el.className='message assistant image-message';const img=document.createElement('img');img.src=url;img.alt=prompt;el.appendChild(img);chat.appendChild(el);chat.scrollTop=chat.scrollHeight}finally{send.disabled=false;status.textContent=''}}
input.onkeydown=e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();composer.requestSubmit()}};input.oninput=()=>{input.style.height='auto';input.style.height=Math.min(input.scrollHeight,180)+'px'};$('imageMode')?.addEventListener('click',()=>{imageMode=!imageMode;$('imageMode').classList.toggle('image-mode',imageMode);input.placeholder=imageMode?'生成したい画像を説明してね…':'メッセージを入力…'});refreshChats().catch(()=>{});api('/api/auth/me').catch(()=>{});